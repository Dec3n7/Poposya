# Зависимости изолированного сервиса рендера (см. server.py). Движок рендера —
# librsvg (системный пакет librsvg2-bin из Dockerfile), а не Python-браузер,
# поэтому здесь только HTTP-сервер. Пины ==.
aiohttp==3.14.1
