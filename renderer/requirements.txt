# Зависимости изолированного сервиса рендера (см. server.py). Держим минимальными:
# чем меньше в контейнере с браузером, тем меньше attack surface. Пины ==; лок с
# хэшами (requirements.lock) генерируется так же, как у бота (см. RUNBOOK).
playwright==1.62.0
aiohttp==3.14.1
