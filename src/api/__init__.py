"""Веб-панель Поповы — REST API (FastAPI), отдельный процесс.

Второй клиент той же бизнес-логики, что и Discord-бот: переиспользует доменные
сущности, use-case'ы и репозитории из `src/`, ничего не дублируя. Бот про этот
пакет не знает и его не импортирует.

Запуск: `uvicorn src.api.main:app` (в проде — свой docker-сервис `api`).
"""
