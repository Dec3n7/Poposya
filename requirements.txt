discord.py[voice]>=2.4
yt-dlp>=2024.10.0
aiohttp>=3.9  # импортируется напрямую (lyrics, spotify, groq, health-сервер)
pydantic-settings>=2.4
sqlalchemy>=2.0
aiosqlite>=0.20
alembic>=1.13
asyncpg>=0.29  # драйвер PostgreSQL; не мешает, пока DATABASE_URL указывает на SQLite
