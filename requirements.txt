# Прод-зависимости закреплены ТОЧНО (==) на протестированных версиях: образ
# пересобирается воспроизводимо, а внезапный релиз апстрима не ломает прод и не
# протаскивает уязвимость незаметно. Обновление — осознанный шаг: поднять версию,
# прогнать тесты (CI), закоммитить. Транзитивные зависимости не пиннятся хэшами —
# при желании ужесточить до полного lock см. RUNBOOK (pip-compile --generate-hashes).
discord.py[voice]==2.7.1   # [voice] тянет PyNaCl (кодек голосового канала)
# yt-dlp ломается на изменениях YouTube чаще прочего — держим свежим и бампим
# регулярно (см. RUNBOOK). Пин ради воспроизводимости; при поломке музыки — вверх.
yt-dlp==2026.7.4
aiohttp==3.14.1            # импортируется напрямую (lyrics, spotify, groq, health-сервер)
pillow==12.3.0            # рендер карточки /rank (шрифт DejaVu ставится в образ)
pydantic==2.13.4          # используется напрямую (model_validator в src/config.py)
pydantic-settings==2.14.2
sqlalchemy==2.0.51
aiosqlite==0.22.1
alembic==1.18.5
asyncpg==0.31.0           # драйвер PostgreSQL; не мешает на SQLite

# веб-панель (отдельный процесс src.api; бот их не импортирует)
fastapi==0.139.2
uvicorn[standard]==0.51.0
pyjwt==2.13.0             # подпись JWT-сессий
